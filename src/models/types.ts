export interface Load {  // interface para representar cada carga de combustible
  km: number;  // propiedad para los kilómetros registrados en la carga
  litros: number;  // propiedad para los litros registrados en la carga
}